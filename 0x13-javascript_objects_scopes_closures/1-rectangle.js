#!/usr/bin/node
class Rectangle {
  // This is a class Rectangle that defines a rectangle:
  width;
  height;

  constructor(width, height) {
  this.width = width;
  this.height = height
  }
}
module.exports = Rectangle;
