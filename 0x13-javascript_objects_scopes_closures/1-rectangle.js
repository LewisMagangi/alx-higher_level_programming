#!/usr/bin/node
class Rectangle {
  // This is a class Rectangle that defines a rectangle:
  width;
  height;

  constructor (w, h) {
    this.w = width;
    this.h = height;
  }
}
module.exports = Rectangle;
