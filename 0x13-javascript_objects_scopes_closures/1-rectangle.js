#!/usr/bin/node
class Rectangle {
  // This is a class Rectangle that defines a rectangle:
  width;
  height;

  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}
module.exports = Rectangle;
